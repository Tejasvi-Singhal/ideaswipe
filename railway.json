{
  "$schema": "https://schema.railway.app/railway.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --legacy-peer-deps && npx ts-node --project tsconfig.server.json socket-server.ts --version || true"
  },
  "deploy": {
    "startCommand": "npx ts-node --project tsconfig.server.json socket-server.ts",
    "restartPolicyType": "ON_FAILURE"
  }
}